#include <stdio.h>
#include <conio.h>

void main()
{
    int a,b,sum;
    printf("Enter two integer numbers:");
    scanf("%d %d",&a,&b);
    sum=a+b;
    printf("the sum is=%d.",sum);
    getch();
    return 0;
}
